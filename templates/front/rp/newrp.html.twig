{% extends 'front/base_front.html.twig' %}

{% block main %}
{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.happensAt) }}
    {{ form_row(form.place) }}
    {{ form_row(form.rpposts.vars.prototype.content) }}
    {{ form_row(form.rpposts.vars.prototype.poster) }}
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}
{% endblock %}